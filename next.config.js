// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: { domains: ['api.maptiler.com'] },
  // devIndicators:  false,
};

module.exports = nextConfig;
